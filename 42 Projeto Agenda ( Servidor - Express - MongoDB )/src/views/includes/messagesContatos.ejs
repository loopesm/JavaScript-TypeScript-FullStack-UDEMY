<% if (errorsContatos.length > 0) { %>

<div class="row">
    <div class="col my-2">
      <div class="alert alert-danger text-center">

        <% errorsContatos.forEach( erro => { %>
            <%= erro %> <br>
        <% }); %>

      </div>
    </div>
  </div>

  <% } %>

  <% if (successContatos.length > 0) { %>

    <div class="row">
        <div class="col my-2">
          <div class="alert alert-success text-center">
    
            <% successContatos.forEach( success => { %>
                <%= success %> <br>
            <% }); %>
    
          </div>
        </div>
      </div>
    
      <% } %>